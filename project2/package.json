{
  "scripts": {
    "start": "npm-run-all --parallel sass:watch pug:watch",
    "pug:watch": "pug templates -w -P",
    "sass": "gulp sass",
    "sass:watch": "gulp sass:watch"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1",
    "npm-run-all": "^4.1.2"
  }
}
